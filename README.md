# تَـراصُـف — تسوية حسابات الموردين

إصدار الواجهة 0.4.6 — مرشح محلي غير منشور — نسخة تجريبية عربية لمقارنة كشف المورد وتقرير الحسابات الدائنة داخل المتصفح. استيراد XLSX/CSV وPDF نصي، تعبئة ما يمكن قراءته من الملفين، مطابقة محافظة، مراجعة يدوية، وجسر حسابي اختياري وتصدير Excel. راجع [طريقة PDF وحدوده وأدلة الاختبار](docs/PDF.ar.md).

**دورة المحرك 0.4.6:** فرع تطوير محلي `work/reliability-046` دون دمج أو نشر. مجموعات دفع بهوية صريحة، قراءة PDF عربي موثقة، ترحيل قوالب الأعمدة، وحاجز يمنع استخدام صيغة افتراضية عند تعذر التحقق من القراءة. النتائج والحدود في [تقرير الدورة](docs/ENGINE-RELIABILITY-0.4.6.ar.md).

**تدقيق المحرك 0.4.5:** إصلاحات للهوية ونوع التقرير ومعنى المبلغ وقراءة Excel/PDF، مع مولّد مستقل و5,000 حالة فعلية وفحص مستقل لملفات التصدير. النتائج صناعية ضمن نطاق معلن؛ الصور العربية تبقى مسودات غير معتمدة. [التدقيق وحدود الدعم وأوامر التشغيل](docs/ENGINE-RELIABILITY-0.4.5.ar.md).

**النصوص العربية 0.4.4:** مراجعة العناوين والشرح ورسائل الرفع والمراجعة والمساعد والتصدير بلغة أوضح وأقرب للمحاسب، مع توحيد المصطلحات والحفاظ على حدود المحرك والخصوصية. لا تغيير في حساب المبالغ أو قواعد المطابقة. [دليل الكتابة](docs/ARABIC-VOICE.ar.md) و[نطاق التدقيق والتحقق](audit/COPY-0.4.4.ar.md).

**المشهد والمميزات 0.4.3:** ورقتان بنسبة A4، ومسح مستقل داخل كل ورقة، وفصل محسوب عن فتحة المعالجة. رسوم ذات عمق تبرز الخصوصية وأدلة النتائج وخطوات العمل والمساعد المحلي، مع ظهور متدرج أثناء التمرير وإيقاف الحركة. [الخطة ومعايير القبول](audit/VISUAL-0.4.3.ar.md).

**صقل الخط 0.4.1:** الاسم والعناوين نصوص حقيقية بخط Thmanyah Serif Display Bold، بدل صور الحروف. ملف WOFF2 مستضاف محليًا، وقد طابقت منحنياته وجداول تشكيل العربية وبيانات إصداره `1.003` ملف ثمانية الأصلي دون تعديل. العناوين تلتف طبيعيًا بحجم موحّد لدورها، ويقتصر التطويل على الاسم **تَـراصُـف**. لا CDN للخطوط. [مصدر الملف والتحقق منه](public/fonts/Thmanyah-PROVENANCE.txt).

**هوية تراصف:** خلفية بيضاء، شعار الشريطين وتدرج أزرق–بنفسجي، وعناوين ثمانية مع نصوص Noto Sans Arabic محلية. مقدمة تختفي مع بدء الجلسة، ومشهد مستندات قابل لإيقاف الحركة، وتحسينات الجوال والتنقل بلوحة المفاتيح. أضيف مشهد بوابة متجهي متناسب، اسم عربي فقط، فوائد واضحة وتفاصيل الخصوصية في نهاية الصفحة، وبطاقتا رفع مع رفض الإسقاط المتعدد دون فقد الملفات. [تدقيق الواجهة وخطة التحقق](audit/VISUAL-0.4.2.ar.md). إصدار المحرك الحالي وتوافق الجلسات `0.3.10-experimental`. [تفاصيل الهوية والتحقق](docs/TARASUF-IDENTITY.ar.md).

**تحديث 0.3.9:** إصلاح خطأ PDF قد يخفي فشلًا متأخرًا ويعيد صفوفًا جزئية، وأسئلة فرق مبلغ محدد تستند إلى أدلة المحرك بالعربية والإنجليزية. مساعد الصور الجديد يقرأ PNG/JPEG وPDF الممسوح محليًا كمسودة تجريبية مرتبطة بموضع الكلمة؛ لا يدخل أرقامها في المحاسبة أو التصدير. أظهر الاختبار ضعفًا في الأرقام العربية الشرقية، وسُجل كفجوة مفتوحة. [الاختبارات والحدود](audit/VISUAL-AI-0.3.9.ar.md).

**مراجعة وإصلاح 0.3.5:** تحديث Claude مع إصلاح توافق XML في ملفات XLSX الأصلية، وتجميع عناوين PDF الممتدة مع حفظ مصدرها، ومراجعة PDF قابلة للنقر والتراجع عن تعديل الحدود. ملخص التأكيد يخفي التفاصيل الاختيارية في «خيارات متقدمة». اتجاه المدين والدائن يثبت من سلسلة الأرصدة أو يحتاج اختيارًا واحدًا؛ خطأ قراءة في أي طرف يمنع المطابقات الآلية. [الخطة](docs/REASONABLE-SIMPLIFICATION.ar.md) و[الأسباب والاختبارات والحدود](audit/SIMPLICITY-0.3.5.ar.md).

**تأكيد أسرع 0.3.4:** حقلان ظاهران للحالات الواضحة، وتعبئة البيانات الصريحة وصيغ القراءة غير الملتبسة واقتراح حدود PDF. الأسماء اختيارية لمقارنة الحركات؛ الأرصدة عند طلب تسويتها فقط. 234 اختبارًا ناجحًا محليًا. [السلوك والضوابط وأدلة التحقق](audit/CONFIRMATION-0.3.4.ar.md).

**إصلاح الرفع 0.3.3:** 203 اختبارات ناجحة محليًا، وإصلاح خلفيات PDF والترويسات الملونة، وفحص ردود قارئ الملفات وإعادة تهيئته بعد الفشل. تحققت قراءة الملفين المرفقين، وطابقت الحركات والأرصدة في PDF أداة مستقلة. [الأسباب والأدلة وحدود الإثبات](audit/IMPORT-0.3.3.ar.md). بوابة GitHub تختبر الرفع والتعافي والتصدير قبل النشر.

**إصلاح الرفع 0.3.2:** 189 اختبارًا و10 أخطاء مقصودة تكشفها الاختبارات. قبول مصنفات حتى 40 ورقة ضمن سقف الحجم والخلايا، واقتراح جدول المصدر المناسب، وفحص Excel على مستوى الخلايا المختارة، وتصحيح قراءة حدود جداول PDF. [الأسباب والإصلاحات والاختبارات](audit/IMPORT-0.3.2.ar.md).

**سجل تشديد المحرك 0.3.1:** 167 اختبارًا، منها 48 تركيبة ملفات اصطناعية كاملة، وسبعة أخطاء مقصودة تكشفها بوابة Mutation Testing، وفحص مستقل لـ18 ملف Excel. ضوابط إضافية لتنسيقات Excel ورسوم PDF والتأكيدات الصريحة ومنع اختيار AI لمرجع ملتبس. التفاصيل وحدود الإثبات في [تقرير المحرك](audit/ENGINE-0.3.1.ar.md). يلزم إعادة رفع المصادر عند الانتقال من جلسة محفوظة بإصدار أقدم.

**حالة الإصدار — 9 سبتمبر 2026:** نُشرت [النسخة التجريبية](https://mjodshamrani-ux.github.io/accounting/) من المصدر `a8977ba8df739ddad576ed556bd31fc9ff1dc4f9`. نجح [سير عمل GitHub](https://github.com/mjodshamrani-ux/accounting/actions/runs/34286918090): 102 اختبارًا، وفحص 18 ملف Excel بأداة مستقلة، وفحص تبعيات الإنتاج وفحص الأنواع والبناء واختبار Chromium، ثم النشر. يتضمن اختبار المتصفح رفع PDF وتصدير Excel وإعادة فتحه والعمل بعد قطع الاتصال. فُحص المثال على الموقع المنشور: مطابقتان آليتان و10 حركات دون مقابل؛ عدم تأكيد التغطية أبقاه مقارنة حركات فقط.

المساعد العربي الحالي يعتمد قواعد وأدلة المحرك. نموذج الجهاز اختياري للأسئلة الإنجليزية فقط عند توفره مسبقًا؛ لم يُختبر نموذج AI فعلي بعد. نجاح الاختبارات الاصطناعية لا يثبت دقة ميدانية أو سلامة كل ملف PDF.

جولة الضغط السابقة: 79 اختبارًا ناجحًا وفحص مستقل لـ18 ملف Excel؛ نتائجها التاريخية والعيوب المصححة وحدود الإثبات محفوظة في [تقرير الضغط](audit/stress/REPORT.ar.md).

**نسخة تجريبية لم تثبت دقتها على عينات ميدانية مستقلة.** لا ندّعي صحة 100% ولا اعتمادًا محاسبيًا. راجع [الخطة](docs/TECHNICAL_PLAN.ar.md) و[التحقق والحدود](docs/VALIDATION.ar.md) قبل استخدام بيانات حقيقية. المستودع المعتمد: [mjodshamrani-ux/accounting](https://github.com/mjodshamrani-ux/accounting). حالة النشر والاختبارات تظهر في تبويب Actions.

## تشغيل محلي

Node 24 وpnpm 11.19.0:

```sh
pnpm install --frozen-lockfile
pnpm dev
```

## التحقق والبناء

```sh
pnpm typecheck
pnpm test
pnpm test:mutations
pnpm build
pnpm exec playwright install chromium
pnpm test:browser
```

آخر أمر يحتاج بيئة تسمح بتشغيل Chromium. حزمة الاختبار تستخدم بيانات اصطناعية وتخدم `dist` تحت مسار فرعي يحاكي GitHub Pages. فشل تشغيل المتصفح بسبب قيود النظام ليس نجاحًا للاختبار.

```sh
node scripts/serve-static.mjs
```

يفتح آخر أمر نسخة الإنتاج على `http://127.0.0.1:4173/mizan-test/`.

## GitHub Pages

1. استخدم مستودع المشروع المقصود، وارفع مصدر هذا المجلد دون node_modules أو ملفات محاسبية فعلية.
2. من Settings → Pages اختر Source: GitHub Actions. لا تغير خصوصية المستودع إلا بقرار صاحبه؛ إتاحة Pages تعتمد على الخطة ونوع المستودع.
3. سير العمل `.github/workflows/pages.yml` يختبر ويُنشئ الموقع ثم ينشره عند الدفع إلى main/master، أو بالتشغيل اليدوي.
4. النشر العام لموقع أعمال مدفوع مستقبلًا يحتاج مراجعة شروط GitHub Pages وملاءمة الاستضافة. النسخة الحالية أداة تجريبية ثابتة بلا تحصيل مدفوعات.
5. الأصول نسبية لتعمل عند جذر الحساب أو تحت `/repository/`. مخرجات النشر في `dist` فقط.

## الخصوصية

لا backend مالي، ولا telemetry، ولا خطوط خارجية. العامل المحلي يحمّل قبل اختيار البيانات ويظل جاهزًا للمقارنة والتصدير. لا تُحفظ المعاملات في التخزين المحلي؛ تعيين الأعمدة فقط اختياري. تنزيل Excel يحفظ المعلومات على جهازك حيث تختار.

يحمّل مساعد OCR عند طلبه ملفات برنامج ونماذج ثابتة من نفس الموقع؛ لا يتضمن طلبها محتوى المستند. لا توجد مفاتيح أو اتصالات نموذج سحابي. تُضاف CSP إلى نسخة البناء وتمنع اتصالات التطبيق المباشرة؛ يسمح wasm-unsafe-eval بتشغيل القارئ المحلي دون السماح بتقييم JavaScript العام. GitHub Pages لا يسمح بضبط كل رؤوس HTTP؛ لا ندّعي عزلًا أمنيًا كاملًا للعامل أو تحكمًا بسجلات المضيف. سياسة CSP ليست إثباتًا منفردًا للخصوصية.

## الحدود

- 8 MB لكل ملف، 100 عمود، نحو 20,000 صف، 40 ورقة، ومليون خلية للمصنف كاملًا؛ رفض بدل اقتطاع صامت.
- ملفات XLSX المشفرة وذات روابط خارجية/ماكرو مرفوضة. الصيغ في الأعمدة المختارة تتطلب قيمًا ثابتة موثوقة أو استبعادًا صريحًا؛ معادلات الأعمدة الجانبية غير المستخدمة لا تمنع القراءة.
- CSV بترميز UTF-8 وفاصلة/فاصلة منقوطة/Tab، بلا تنفيذ نصوص كصيغ.
- اتجاه الإشارات ونوع التقرير والنطاق قابلة للتعديل، ومعروضة في ملخص كل ملف. لا ادعاء بفهم آلي عام للملفات.
- صفوف الإجمالي والرصيد وصفوف العناوين المكررة تُستبعد تلقائيًا بسبب مسجَّل يبقى في التصدير؛ لا يُحذف صف بلا أثر. الصف غير المقروء يُبلَّغ عنه ويُعدّ، ويمنع المطابقات الآلية وأي تسوية أرصدة متحققة.
- معنى الأعمدة يستدل من العناوين الصريحة؛ شكل القيمة وحده لا يثبت أنها مبلغ أو مرجع أو تاريخ حركة. الالتباس يحتاج اختيارك.
- مطابقة آلية لمرجع أصلي متطابق يحوي أحرفًا وأرقامًا وغير مكرر في أي طرف، ومبلغ موقّع مطابق ضمن نافذة تاريخ. توحيد الفواصل والرموز يولّد مرشحين للمراجعة فقط. المراجع الرقمية فقط للمراجعة.
- PDF النصي متاح للتسوية ضمن [حدود القراءة والمراجعة](docs/PDF.ar.md). الصور وPDF المصور لها مسودة OCR تجريبية منفصلة، حتى 5 صفحات و8 MB؛ ليست مصدرًا محاسبيًا. لا N:M ولا تخصيص دفعات ولا ترحيل قيود.
- إعادة فتح الموقع دون اتصال غير مدعومة؛ التشغيل المحلي بعد تحميل المحرك متاح. العامل مضمّن مع الصفحة ويمكن إعادة إنشائه من النسخة المحملة بعد الإلغاء.

`examples/` يحتوي بيانات اصطناعية فقط. لا تستخدمه دليلاً على دقة ميدانية.

## المساعد المحلي وAI — الحالة الحالية

«اسأل عن النتيجة» يشرح الأرقام من نتيجة المحرك ويذكر صفوف المصدر. لا تخرج منه قرارات مطابقة. تمسح المحادثة عند تغيير النتيجة ولا تُخزن على الخادم أو في التخزين المحلي.

الأسئلة الإنجليزية غير المفهومة يمكن توجيهها إلى Prompt API في المتصفح فقط إذا أعادت حالة `available` للإنجليزية في الإدخال والإخراج. الأسئلة العربية تبقى في مساعد القواعد. لا يوجد اتصال بنموذج سحابي أو مفتاح API أو تنزيل يبدأه التطبيق. إن لم يتوافر النموذج، يبقى مساعد القواعد ويصرّح بحدوده. لا نضمن توفر النموذج على كل جهاز، ولم يُختبر نموذج فعلي ضمن أدلة هذا الإصدار.

مخرجات النموذج JSON محدودة: نوع السؤال، معرف حركة، ومعرفات اقتراح اختياري. يُرفض أي حقل أرقام أو اعتماد أو نص حر. المحرك يعيد اختبار معرفات الحركات والمبالغ والإشارات وعدم إعادة الاستخدام. تساوي المجاميع وحده يبقي الفرضية للمراجعة. الأسئلة والنتائج لا تُعامل كتعليمات تنفيذ.

توثيق واجهة المتصفح: https://developer.chrome.com/docs/ai/prompt-api

قبل كل تصدير، يعاد بناء النتيجة من ملفات المصدر وتعيين الأعمدة والقرارات. يرفض التصدير النتيجة المختلفة، وتضاف ورقتا Diagnostics وMatch evidence للتدقيق.

## سجل تحديث 0.3 السابق

أُصلحت قراءة أرقام Excel الأصلية وإسناد مراجع الشات، وأضيف تحقق فعلي لفك الضغط، وبصمات الملفات وإعادة قراءتها من أصلها قبل التصدير، ومراجعة الطرفين وسجل قرارات وحفظ/استعادة جلسة محلية. ملف الجلسة يتضمن البيانات دون تشفير؛ لا تشاركه كمستند عام.

المساعد العربي يعتمد القواعد. لا يستدعي التطبيق نموذج العربية غير المدعوم. لا يوجد نموذج عربي توليدي مثبت. العامل الحالي Blob مضمّن تحت CSP الصفحة، بدل ملف عامل مستقل.

فحص تلك المرحلة: صفر تنبيهات تبعيات، و68 اختبارًا ناجحًا. تفاصيل الشروط والحدود محفوظة في [تقرير إغلاق التدقيق](audit/RESOLUTION-0.3.ar.md). حالة الإصدار الأحدث موضحة في مقدمة هذا الملف؛ النتائج السابقة لا تُحذف ولا تُنسب إلى الإصدار الأحدث.
